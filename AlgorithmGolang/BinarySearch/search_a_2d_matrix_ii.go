package BinarySearch

// * 编写一个高效的算法来搜索 m x n 矩阵 matrix 中的一个目标值 target 。该矩阵具有以下特性：
// * 每行的元素从左到右升序排列。
// * 每列的元素从上到下升序排列。

// * m == matrix.length
// * n == matrix[i].length
// * 1 <= n, m <= 300
// * -10^9<= matrix[i][j] <= 10^9
// * -10^9<= target <= 10^9

// * 如果大于target，则左上肯定不在搜索范围

func SearchMatrixII(matrix [][]int, target int) bool {
	return false
}
